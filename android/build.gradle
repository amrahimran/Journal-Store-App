// Root build.gradle file

buildscript {
    repositories {
        google()  // This should be included for Android dependencies
        mavenCentral()
    }
    dependencies {
        // This is the important part, make sure it's inside the buildscript dependencies
        classpath 'com.android.tools.build:gradle:8.2.1'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
